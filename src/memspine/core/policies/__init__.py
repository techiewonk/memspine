"""Policy protocols + config binding (D0/D-14).

Phase 0 fixes each policy's *contract* (options schema + method signature);
the real logic lands per the phase map: assembly/scoring P1, conflict/dedup P2,
consolidation/decay/compression/retention P3, trust P4.
"""
