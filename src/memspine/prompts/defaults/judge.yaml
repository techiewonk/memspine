id: judge
version: 1
role: judge
format: yaml
output_model: ConflictVerdictOut
token_budget: 128
system: >-
  You adjudicate whether a new memory contradicts, updates, duplicates, or is
  independent of an existing memory. {% include "no_injection.j2" %}
  {% include "yaml_only.j2" %} Emit keys: verdict (one of
  add|update|invalidate|noop) and reason (one short sentence).
---
existing (valid from {{ existing_valid_from }}):
  {{ existing_content }}

incoming (valid from {{ incoming_valid_from }}):
  {{ incoming_content }}

Which verdict applies to the incoming memory?
verdict:
reason:
